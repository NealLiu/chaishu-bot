{
  "name": "scripts",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "node --test tests/",
    "test:all": "npm run test && bash test-pipeline.sh --ci",
    "lint": "eslint .",
    "lint:fix": "eslint --fix .",
    "format": "prettier --write '**/*.{js,mjs,json,md}' --ignore-path ../.gitignore 2>/dev/null || prettier --write '**/*.{js,mjs}'",
    "format:check": "prettier --check '**/*.{js,mjs}'"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "type": "commonjs",
  "dependencies": {
    "@larksuiteoapi/node-sdk": "^1.71.1",
    "puppeteer": "^25.3.0",
    "ws": "^8.21.1",
    "yaml": "^2.9.0"
  },
  "devDependencies": {
    "@eslint/js": "^10.0.1",
    "eslint": "^10.7.0",
    "eslint-config-prettier": "^10.1.8",
    "prettier": "^3.9.5"
  }
}
